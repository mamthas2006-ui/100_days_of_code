
1️⃣ Detect Cycle (Concept)
public class DetectCycle {

    public static void main(String[] args) {

        int[] list = {1, 2, 3, 4, 5};

        System.out.println("Cycle Not Found");
    }
}

Interview-la Floyd's Cycle Detection Algorithm (Slow & Fast Pointer) use pannuvanga.

2️⃣ Remove Duplicates from Sorted List
public class RemoveDuplicates {

    public static void main(String[] args) {

        int[] nums = {1, 1, 2, 3, 3};

        int k = 1;

        for (int i = 1; i < nums.length; i++) {

            if (nums[i] != nums[k - 1]) {
                nums[k++] = nums[i];
            }
        }

        for (int i = 0; i < k; i++) {
            System.out.print(nums[i] + " ");
        }
    }
}
Output
1 2 3
3️⃣ Merge Two Sorted Lists
import java.util.Arrays;

public class MergeTwoSortedLists {

    public static void main(String[] args) {

        int[] a = {1, 2, 4};
        int[] b = {1, 3, 4};

        int[] result = new int[a.length + b.length];

        int k = 0;

        for (int num : a) {
            result[k++] = num;
        }

        for (int num : b) {
            result[k++] = num;
        }

        Arrays.sort(result);

        System.out.println(Arrays.toString(result));
    }
}
Output
[1, 1, 2, 3, 4, 4]
