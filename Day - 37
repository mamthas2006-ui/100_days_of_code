1️⃣ Binary Search — LeetCode #704 ⭐
class Solution {
    public int search(int[] nums, int target) {

        int left = 0;
        int right = nums.length - 1;

        while (left <= right) {

            int mid = left + (right - left) / 2;

            if (nums[mid] == target) {
                return mid;
            }

            if (nums[mid] < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }

        return -1;
    }
}

Example:

nums = [-1,0,3,5,9,12]
target = 9

Output = 4

Time: O(log n)

2️⃣ Find First and Last Position — LeetCode #34 ⭐⭐⭐
class Solution {

    public int[] searchRange(int[] nums, int target) {

        int first = findFirst(nums, target);
        int last = findLast(nums, target);

        return new int[]{first, last};
    }

    private int findFirst(int[] nums, int target) {

        int left = 0;
        int right = nums.length - 1;
        int answer = -1;

        while (left <= right) {

            int mid = left + (right - left) / 2;

            if (nums[mid] == target) {
                answer = mid;
                right = mid - 1;
            }
            else if (nums[mid] < target) {
                left = mid + 1;
            }
            else {
                right = mid - 1;
            }
        }

        return answer;
    }

    private int findLast(int[] nums, int target) {

        int left = 0;
        int right = nums.length - 1;
        int answer = -1;

        while (left <= right) {

            int mid = left + (right - left) / 2;

            if (nums[mid] == target) {
                answer = mid;
                left = mid + 1;
            }
            else if (nums[mid] < target) {
                left = mid + 1;
            }
            else {
                right = mid - 1;
            }
        }

        return answer;
    }
}

Example:

Input:
nums = [5,7,7,8,8,10]
target = 8

Output:
[3,4]
3️⃣ Search in Rotated Sorted Array — LeetCode #33 ⭐⭐⭐
class Solution {

    public int search(int[] nums, int target) {

        int left = 0;
        int right = nums.length - 1;

        while (left <= right) {

            int mid = left + (right - left) / 2;

            if (nums[mid] == target) {
                return mid;
            }

            // Left half is sorted
            if (nums[left] <= nums[mid]) {

                if (nums[left] <= target && target < nums[mid]) {
                    right = mid - 1;
                } else {
                    left = mid + 1;
                }

            }
            // Right half is sorted
            else {

                if (nums[mid] < target && target <= nums[right]) {
                    left = mid + 1;
                } else {
                    right = mid - 1;
                }
            }
        }

        return -1;
    }
}

Example:

Input:
nums = [4,5,6,7,0,1,2]
target = 0

Output:
4
📄 README.md
# Day 37

## Topic
Binary Search

## Problems
- Binary Search - LeetCode #704
- Find First and Last Position - LeetCode #34
- Search in Rotated Sorted Array - LeetCode #33

## Concepts
- Binary Search
- Lower Bound
- Upper Bound
- Rotated Array

✅ Day 37 Completed
💾 Commit
git add .
git commit -m "Day 37: Solved Binary Search Problems"
git push
🎯 Today's order

#704 → #34 → #33

First #704 perfect-aa understand pannu. Adhu dhaan remaining two problems-ku base. 🔥

Day 37/100 ✅
